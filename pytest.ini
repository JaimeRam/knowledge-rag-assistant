[pytest]
asyncio_mode = auto
testpaths = tests
addopts = --ignore=tests/test_ingestion.py --tb=short
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: tests requiring live external services (DAPI, Voyage AI)
